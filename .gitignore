# Personal inventory / financial tracking files — never publish these
inventory-template.csv
inventory-template.xlsx
DTP Inventory.xlsx
~$*.xlsx
*.xlsx
.DS_Store

# Whatnot purchase data / cost basis — private business data, never publish
whatnot/
order-report-*.csv

# Weekly report output — cost basis and inventory snapshots, never publish.
# The script itself is tracked; its data files are not.
reports/*.csv
reports/*.json

# The daily briefing's launchd log — it contains the briefing itself, which
# lists costs and break-evens for live inventory.
reports/*.log
